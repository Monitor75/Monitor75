from prometheus_client import start_http_server, Counter, Gauge

chunk_writes = Counter('storage_chunk_writes_total', 'Total chunk writes')
chunk_reads = Counter('storage_chunk_reads_total', 'Total chunk reads')
active_connections = Gauge('storage_active_connections', 'Active connections')

def start_metrics_server(port=8000):
    start_http_server(port)       # Then in storage_api.py, import and increment counters as appropriate.
